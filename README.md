To run the code, use the following commands


For dummy server

cd ./goServer && go run .



For my solution


npm run dev

or 

npm build && npm start


All the APIs are available at localhost:3000/api/{path}